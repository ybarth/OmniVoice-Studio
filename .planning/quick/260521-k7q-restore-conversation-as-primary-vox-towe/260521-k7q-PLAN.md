---
phase: quick-260521-k7q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.jsx
  - frontend/src/components/Header.jsx
  - frontend/src/components/NavRail.jsx
  - frontend/src/index.css
  - frontend/src/store/uiSlice.ts
  - frontend/src/store/store.test.js
autonomous: true
requirements:
  - Restore the Conversation tab to the focused Vox Tower shell.
  - Make Conversation the first, primary, and default mode.
  - Keep the other stripped scope intact: Clone, Design, Drive, and Settings only.
  - Preserve the existing ConversationTab behavior rather than reimplementing it.
  - Keep mobile navigation readable with five tabs.
---

<objective>
Correct the focused preview so Conversation is again the central/default app
surface while retaining the reduced Vox Tower shell.
</objective>

<design>
Use the existing `ConversationTab` component exactly as the restored feature
surface. Add `conversation` back into the focused mode allow-list, render it
before all other modes, and make it the store fallback/default. Add it as the
first nav item and header metadata entry. Adjust only the mobile nav sizing
needed to fit five tabs.
</design>

<verify>
- `bun run --cwd frontend test src/store/store.test.js` red before production changes.
- `bun run --cwd frontend test src/store/store.test.js` green after changes.
- `bun run --cwd frontend test`
- `bun run test:frontend`
- `bun run --cwd frontend typecheck:ci`
- `bun run --cwd frontend build`
- Playwright desktop and iPhone screenshots confirm Conversation is active/default.
</verify>
