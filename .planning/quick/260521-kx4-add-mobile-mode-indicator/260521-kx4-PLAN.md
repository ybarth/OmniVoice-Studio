---
phase: quick-260521-kx4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Header.jsx
  - frontend/src/index.css
autonomous: true
requirements:
  - Make the upper mobile chrome clearly show the active mode.
  - Make it obvious that the phone/mobile layout is active.
  - Avoid desktop visual clutter and preserve the existing focused shell.
---

<objective>
Add a clear mobile-only indicator near the top of the app so the user can
confirm both the current mode and that the mobile form is active.
</objective>

<design>
Keep the existing mode breadcrumb visible in the header and add a phone-only
badge beside it that reads `iPhone mobile form` on iPhone. The badge is
rendered from the existing device profile so desktop stays unchanged. Mobile
CSS gives the left header cluster enough width for the mode and badge without
creating horizontal overflow.
</design>

<verify>
- Playwright iPhone-width sweep at 320px across Conversation, Clone, Design,
  Drive, and Settings.
- `bun run --cwd frontend test`
- `bun run test:frontend`
- `bun run --cwd frontend typecheck:ci`
- `bun run --cwd frontend build`
</verify>
