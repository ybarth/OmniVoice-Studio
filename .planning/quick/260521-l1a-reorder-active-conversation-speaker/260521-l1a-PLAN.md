---
phase: quick-260521-l1a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/ConversationTab.jsx
  - frontend/src/utils/conversationSession.ts
  - tests/frontend/conversationSession.test.mjs
autonomous: true
requirements:
  - When a speaker pill is clicked at the top of Conversation, that speaker's
    profile card should move to the top of the speaker setup list.
  - Preserve each speaker's saved settings.
  - Avoid changing export or archive ordering as a side effect.
---

<objective>
Make the Conversation speaker setup list follow the currently selected speaker
so the active profile is always the first profile card.
</objective>

<design>
Add a pure ordering helper that returns a visual-only active-first copy of the
speaker array. Use that ordered copy only for rendering speaker profile cards.
The stored speaker memory remains unchanged, so turns, export filters, and
conversation archive data keep their existing order.
</design>

<verify>
- Red: `bun run test:frontend` fails because the new helper does not exist.
- Green: `bun run test:frontend`
- Playwright mobile check clicks Speaker 2 and Speaker 1 and verifies the first
  profile card changes accordingly.
- `bun run --cwd frontend test`
- `bun run --cwd frontend typecheck:ci`
- `bun run --cwd frontend build`
</verify>
