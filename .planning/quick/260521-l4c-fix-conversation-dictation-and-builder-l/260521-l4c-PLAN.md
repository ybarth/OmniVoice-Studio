---
phase: quick-260521-l4c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Header.jsx
  - frontend/src/pages/ConversationTab.jsx
  - frontend/src/utils/dictationSettings.ts
  - tests/frontend/dictationSettings.test.mjs
autonomous: true
requirements:
  - Rename the header kicker from Studio to Builder on the builder modes.
  - Make Conversation's Dictate Text control usable from a blank turn.
  - Preserve existing record/upload/transcribe behavior for captured turn audio.
---

<objective>
Update the builder label language and make Conversation dictation work as a
direct microphone-to-text path.
</objective>

<design>
Change the `Header` metadata for Conversation, Clone, and Design from
`Studio` to `Builder`. For Conversation dictation, keep the existing
`/transcribe` endpoint and local dictation settings. If turn audio exists,
Dictate Text transcribes it as before. If no turn audio exists, Dictate Text
starts microphone recording, changes to Stop Dictation, and transcribes the
captured audio automatically when stopped.
</design>

<verify>
- Red: `bun run test:frontend` fails because the new dictation control helper
  does not exist.
- Green: `bun run test:frontend`
- Playwright fake-microphone flow verifies fresh Dictate Text starts recording,
  Stop Dictation posts to `/transcribe`, and returned text enters the textarea.
- Playwright wide desktop check verifies the header shows Builder.
- `bun run --cwd frontend test`
- `bun run --cwd frontend typecheck:ci`
- `bun run --cwd frontend build`
</verify>
