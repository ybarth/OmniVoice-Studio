# Quick Plan: Add pause/resume playback controls

## Scope

- Add pause/resume/replay state for generated clone prompt playback.
- Add pause/resume/replay controls for synthesized conversation turns.
- Keep synthesis, translation, and export behavior unchanged.

## Verification

- Add focused frontend tests for playback control labeling/state.
- Run the frontend test suite.
- Run the frontend build if the test pass is clean.
